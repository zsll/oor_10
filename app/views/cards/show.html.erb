<p id="notice"><%= notice %></p>
<% @card.uploads.each do |u| %>
	<%= link_to image_tag(u.pic.url(:small)), u.pic.url(:large) %>
<% end %>

<%= link_to 'Edit', edit_card_path(@card) %> |
<%= link_to 'Back', cards_path %>
